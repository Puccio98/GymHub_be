export class TokenDao {
    // region Public Methods
    static async create(token: string): Promise<any> {
        // const res: UserItem[] = await db('User').where({email: email}).select('*');
        // return res[0];
    }

    //endregion
}